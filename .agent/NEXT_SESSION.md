# Resume Javelle

1. Run `sudo su` before every operation, then verify `git status --short --branch` and read `.agent/STATE.md`.
2. Read the final stage-exit section of `.agent/reports/P00-REVIEW.md`; P00 is independently accepted and P01 is READY.
3. Preserve the P00-10 boundary: verified `interrupt_agent` is the user-authorized termination/close semantic for this environment; no dedicated close/delete API or record deletion is claimed.
4. Split P01 official-version, dependency, and licensing research into non-overlapping read-only work packages using `.agent/reports/P00-SPEC-COVERAGE.md` routing.
5. Freeze public dependency/version contracts before any P02 build implementation; require an independent P01 reviewer before acceptance.
