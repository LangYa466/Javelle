# Resume Javelle

1. Run `sudo su` before every operation, then verify `git status --short --branch` and read `.agent/STATE.md`.
2. Read the authoritative repair decision at the end of `.agent/reports/P00-REVIEW.md`; P00 is rejected, not accepted.
3. Repair `P00-04` with one genuine read-only recon agent whose attributable return includes its unique ID, read scope, and environment summary.
4. For `P00-10`, verify a genuine platform close operation. The current collaboration tools expose interrupt but no close/delete operation. If the specification is unchanged, P00 and therefore P01 remain blocked until that platform capability exists.
5. Rerun independent P00 review. Only an accepted review may unlock the first READY P01 work package.
