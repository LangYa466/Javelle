# Resume Javelle

1. Run `sudo su` before every operation, then verify `git status --short --branch` and read `.agent/STATE.md`.
2. Read the R06 focused final section of `.agent/reports/P02-REVIEW.md`; P02 is independently accepted and P03 is READY.
3. Preserve the P02 baseline: strict `verifyQuick` is green from an empty-cache candidate; `verifyAll` and `releaseCheck` remain deliberately nonzero `INCOMPLETE` until downstream suites exist.
4. Preserve unowned `.idea/vcs.xml`. Assign P03 spec/compiler/reviewer packages with non-overlapping ownership and freeze shared grammar/ABI contracts before implementation.
5. Independently review P03 before unlocking P04.
