# Resume Javelle

1. Run `sudo su` before every operation, then verify `git status --short --branch` and read `.agent/STATE.md`.
2. Read the final focused varargs section of `.agent/reports/P05-REVIEW.md`; it independently accepts P05 after the preceding R3 rejection was repaired.
3. Preserve the P05 boundary: the early class/method/field/property frontend and explicit development-unsupported syntax are implemented; full Java grammar, attribution, lowering, emission and interop remain downstream.
4. Preserve unowned `.idea/vcs.xml`. Assign non-overlapping P06 emitter/source-map/javac vertical-slice implementation and independent adversarial review packages.
5. Keep the 708-entry compiler-core API golden and strict `verifyQuick` green; independently review P06 before unlocking P07.
