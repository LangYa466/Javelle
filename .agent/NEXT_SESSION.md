# Resume Javelle

1. Run `sudo su` before every operation, then verify `git status --short --branch` and read `.agent/STATE.md`.
2. Verify `origin`, `main`, and `dev` against `.agent/reports/P00-GIT.md`.
3. Assign an independent reviewer to reproduce P00 evidence and either accept or reopen P00.
4. If accepted, start the first READY P01 work package from the implementation-plan dependency graph.
