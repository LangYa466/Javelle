# P10-R02 exact diagnostics, workspace symbols, cancellation

- Agent/base: `/root/p00_recon` at `307fba6`.
- Status: IMPLEMENTED; independent acceptance remains pending.

## Repairs

- FAIL05: the separate-process diagnostic regression asserts the exact UTF-16 range `(1,17)..(1,18)` for a semicolon after an emoji on CRLF input, URI, source version 1, followed by exactly the newer version 2 clear notification.
- FAIL06: the validated workspace model remains attached to each snapshot fingerprint and its property metadata materially seeds the compiler-AST semantic index. A checked-in valid P08 fixture proves completion and hover for `demo.User.name`; an unknown position returns empty. Analysis invokes no Gradle, network, user program, or processor.
- FAIL08: analysis has cooperative interrupt checkpoints before and after the compiler stage and a package-scoped injectable checkpoint seam. The deterministic test interrupts at the second stage and observes `CancellationException`; a separate-process race asserts `-32800`, no version-1 publication, exactly one winning version-2 diagnostic, and no duplicate terminal response.

## Tests

- Strict targeted/full: `./gradlew --no-daemon --dependency-verification=strict :workspace-model:compileJava :compiler-core:compileJava :language-tooling:spotlessApply :language-server:spotlessApply :language-tooling:test :language-server:test --rerun-tasks`, exit 0; tooling 4/4, server 12/12; `.agent/logs/P10-R02-final.txt`.
- Aggregate/archive: `./gradlew --no-daemon --dependency-verification=strict verifyQuick :language-server:distZip`, exit 0; `.agent/logs/P10-R02-verify-archive.txt`.
- Artifact: `language-server/build/distributions/teyru-lsp-0.1.0-SNAPSHOT.zip`.
- Cancellation/change race rerun: `:language-server:test --rerun-tasks`, exit 0, 12/12; `.agent/logs/P10-R02-cancel-race.txt`.

## Observed build interference

Two initial runs observed partial `compiler-core`/`workspace-model` class outputs while other shared-worktree builds were active. Explicit dependency compilation removed the race and the identical P10 targets then passed. Logs: `.agent/logs/P10-R02-first.txt`, `.agent/logs/P10-R02-strict.txt`.

## Review boundary

An independent reviewer must still stress the cancel/change ordering and mutate the expected UTF-16 end column before this package can be ACCEPTED. No new LSP method was advertised, so the 95-method inventory is unchanged.
